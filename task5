#include <bits/stdc++.h>
using namespace std;

float Conversion(float n)
{
	return (n - 32.0) * 5.0 / 9.0;
}

int main()
{
	float n;
	cout<<"Enter fahrenheit to convert in celsius : ";
	cin>>n;
	cout << Conversion(n)<<" degree celsius";
	return 0;
}
